import { render, screen } from '@testing-library/react';
import { BannerSlider } from '.';

describe('<BannerSlider>', () => {

    it('should be render a BannerSlider basic', () => {
        render(<BannerSlider></BannerSlider>)
    })
})