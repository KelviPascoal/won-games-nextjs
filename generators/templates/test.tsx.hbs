import { render, screen } from '@testing-library/react';
import { {{pascalCase name}} } from '.';

describe('<{{pascalCase name}}>', () => {

    it('should be render a {{pascalCase name}} basic', () => {
        render(<{{pascalCase name}}></{{pascalCase name}}>)
    })
})